# اختبار سريع لوظيفة الفاتورة

## خطوات الاختبار السريع

### 1. التحقق من Migration
```bash
php artisan migrate
```

### 2. التحقق من الرابط الرمزي
```bash
php artisan storage:link
```

### 3. تشغيل اختبار تلقائي
```bash
php test_invoice_functionality.php
```

## اختبار يدوي من المتصفح

### اختبار 1: طلب جديد مع دفع

1. اذهب إلى الموقع
2. اختر خدمة
3. املأ بيانات الطلب
4. اختر MyFatoorah كطريقة دفع
5. أكمل الدفع
6. بعد نجاح الدفع:
   - ✓ يجب أن ترى رسالة نجاح
   - ✓ يجب أن يتلقى الإدمن إيميل مع الفاتورة
   - ✓ اذهب إلى لوحة التحكم > الطلبات
   - ✓ افتح الطلب الجديد
   - ✓ يجب أن ترى قسم "الفاتورة" مع أزرار العرض والتحميل

### اختبار 2: طلب موجود بدون فاتورة

إذا كان لديك طلب مدفوع بدون فاتورة:

1. اذهب إلى لوحة التحكم > الطلبات
2. افتح الطلب المدفوع
3. في قسم "معلومات الدفع"، يجب أن ترى زر "تحميل الفاتورة من MyFatoorah"
4. اضغط على الزر
5. يجب أن ترى رسالة نجاح
6. يجب أن يظهر قسم "الفاتورة" مع أزرار العرض والتحميل

### اختبار 3: التحقق من قاعدة البيانات

```sql
-- التحقق من الطلبات المدفوعة مع الفواتير
SELECT 
    id,
    order_number,
    payment_status,
    payment_reference,
    invoice_path,
    created_at
FROM orders
WHERE payment_status = 'paid'
ORDER BY created_at DESC
LIMIT 10;
```

### اختبار 4: التحقق من الملفات

```bash
# Linux/Mac
ls -lh storage/app/public/invoices/

# Windows PowerShell
Get-ChildItem -Recurse storage\app\public\invoices\
```

## النتائج المتوقعة

### ✅ نجاح الاختبار يعني:

1. **Migration:**
   - حقل `invoice_path` موجود في جدول `orders`

2. **عند الدفع الناجح:**
   - يتم تحميل الفاتورة تلقائياً من MyFatoorah
   - يتم حفظها في `storage/app/public/invoices/YYYY/MM/`
   - يتم تحديث `invoice_path` في قاعدة البيانات
   - يتم إرسال إيميل للإدمن مع الفاتورة كمرفق

3. **في صفحة الطلبات:**
   - يظهر قسم "الفاتورة" للطلبات المدفوعة
   - يمكن عرض الفاتورة في نافذة جديدة
   - يمكن تحميل الفاتورة

4. **للطلبات بدون فاتورة:**
   - يظهر زر "تحميل الفاتورة من MyFatoorah"
   - يمكن تحميل الفاتورة يدوياً

## المشاكل المحتملة

### ❌ الفاتورة لا تظهر
- تحقق من `invoice_path` في قاعدة البيانات
- تحقق من وجود الملف في التخزين
- تحقق من الرابط الرمزي: `php artisan storage:link`

### ❌ فشل تحميل الفاتورة
- تحقق من `payment_reference` (يجب أن يكون InvoiceId صحيح)
- تحقق من إعدادات MyFatoorah
- تحقق من السجلات: `storage/logs/laravel.log`

### ❌ الإيميل بدون فاتورة
- تحقق من `invoice_path` في الطلب
- تحقق من إعدادات البريد الإلكتروني
- تحقق من السجلات

## سجلات الاختبار

جميع العمليات مسجلة في:
```
storage/logs/laravel.log
```

ابحث عن:
- `MyFatoorah: Downloading invoice`
- `MyFatoorah: Invoice downloaded and saved successfully`
- `Order paid email sent successfully`

## ملاحظات مهمة

1. **في وضع الاختبار:**
   - قد لا تكون جميع الفواتير متاحة فوراً
   - بعض الفواتير قد تحتاج بضع دقائق قبل أن تصبح متاحة

2. **الأمان:**
   - تأكد من أن مجلد `storage/app/public` محمي
   - الفواتير متاحة للعامة عبر الرابط الرمزي

3. **الأداء:**
   - تحميل الفاتورة قد يستغرق 2-5 ثوانٍ
   - إذا فشل، سيتم تسجيل الخطأ في السجلات
