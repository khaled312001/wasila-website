# إعدادات SMTP للإيميلات

## المتطلبات

لضمان وصول جميع الإيميلات للإدارة عبر SMTP، يجب إعداد المتغيرات التالية في ملف `.env`:

```env
MAIL_MAILER=smtp
MAIL_HOST=your-smtp-host.com
MAIL_PORT=587
MAIL_USERNAME=your-email@domain.com
MAIL_PASSWORD=your-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=your-email@domain.com
MAIL_FROM_NAME="منصة وسيلة"
```

## الحالات التي يتم فيها إرسال الإيميلات

1. **عند إنشاء طلب جديد:**
   - للطلبات المدفوعة عبر MyFatoorah: يتم إرسال الإيميل بعد الدفع الناجح
   - للطلبات الأخرى (COD, Card, Bank Transfer): يتم إرسال الإيميل مباشرة عند إنشاء الطلب

2. **عند إرسال رسالة من العميل:**
   - يتم إرسال إيميل للإدارة عند إرسال أي رسالة من العميل

3. **عند إرسال رسالة من نموذج التواصل:**
   - يتم إرسال إيميل للإدارة عند إرسال رسالة من نموذج التواصل

## ملاحظات

- جميع الإيميلات تُرسل إلى البريد الإلكتروني المحدد في إعدادات الموقع (`contact_email`)
- في حالة فشل إرسال الإيميل، يتم تسجيل الخطأ في ملفات الـ Log ولا يتم إيقاف العملية
- يمكن مراجعة ملفات الـ Log للتحقق من نجاح أو فشل إرسال الإيميلات

## اختبار الإعدادات

يمكن اختبار إعدادات SMTP من خلال:
1. إنشاء طلب جديد
2. إرسال رسالة من العميل
3. إرسال رسالة من نموذج التواصل

يجب أن تصل جميع الإيميلات إلى البريد الإلكتروني المحدد في إعدادات الموقع.
